#include <bits/stdc++.h>

using namespace std;

int board[52][52];
int dist[52][52];

int dx[4] = {1, 0, -1, 0};
int dy[4] = {0, 1, 0, -1};

int main(void) {

	int m, n;
	cin >> n >> m;

	for (int i = 0; i < m; ++i) {
		int x, y, a, b;
		cin >> x >> y >> a >> b;

	}

	return 0;
}
